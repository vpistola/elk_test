docker run --rm -v $(pwd)/certs:/certs -it docker.elastic.co/elasticsearch/elasticsearch:8.10.0 bin/elasticsearch-certutil ca --silent --pem -out config/certs/ca.zip; unzip config/certs/ca.zip -d config/certs;

docker run --rm -v $(pwd)/certs:/certs -it docker.elastic.co/elasticsearch/elasticsearch:8.10.0 /usr/share/elasticsearch/bin/elasticsearch-certutil ca --pem --out /certs/ca.zip

docker run --rm -v $(pwd)/certs:/certs -it docker.elastic.co/elasticsearch/elasticsearch:8.10.0 /usr/share/elasticsearch/bin/elasticsearch-certutil ca --pem --out /certs/ca.zip; unzip
