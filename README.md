# Test Elastic cluster (Elasticsearch, Logstash, Kibana)

*Code to be used for testing production Elastic cluster deployment*

## Generate cluster
- Change the values of the .env file
- Run the dc.yml: docker compose -f dc.yml up -d